


<p>画像</p>
  <% if @review.image.attached? %>
    <%= image_tag @review.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
<%= @review.introduction %>